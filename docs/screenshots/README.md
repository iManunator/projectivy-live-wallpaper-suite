# Screenshots

SVG placeholders ship in git so the README renders. Replace with PNG captures from a 1280×720 window after `./scripts/verify.sh`:

- `tonight.svg` — Tonight page with Projectivy overlay
- `gallery.svg` — queue badges + pin / never-show
- `dashboard.svg` — health cards
